// Small pure helpers used to demonstrate unit testing + coverage.

export function add(a: number, b: number): number {
  return a + b
}

export function multiply(a: number, b: number): number {
  return a * b
}

export function isEven(n: number): boolean {
  return n % 2 === 0
}
